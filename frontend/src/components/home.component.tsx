import React from "react";

export const Home = () => {
  return (
    <div className="alert alert-success" role="alert">
      <h4 className="alert-heading">Hello!</h4>
      <p>The home page is still waiting for implementation!</p>
    </div>
  );
};
